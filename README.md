# Cli_Guess
CLI based number guessing game written in Rust 


## 🧩 Installation

Make sure you have [Rust](https://rustup.rs) installed.

Then install this CLI tool globally:

```bash
cargo install --git https://github.com/spam-codes/Cli_Guess

